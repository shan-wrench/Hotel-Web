// src/components/pages/Home.js
import React from 'react'
import Hero from '../Hero' // Make sure this path points to the correct location of Hero

const Home = () => {
  return (
    <div>
      <Hero /> {/* Ensure that Hero component is used correctly */}
    </div>
  )
}

export default Home
